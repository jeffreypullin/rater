# mcmcm_diagnostics errors usefully with the hierarchical DS model

    Code
      mcmc_diagnostics(hds_fit)
    Error <simpleError>
      theta cannot be extracted from the Hierachical Dawid-Skene model.
      Consider using `pars = c('pi')`.

