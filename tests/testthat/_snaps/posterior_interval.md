# posterior_interval errors informatively with the HDS

    Code
      posterior_interval(hds_fit)
    Condition
      Error:
      ! theta cannot be extracted from the Hierachical Dawid-Skene model.
      Consider using `pars = c('pi')`.

